# 运行数据与机密（绝不入库）
data/
.test-data/
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.log
.env
.env.local

# Android 构建产物与签名（构建产物体积大且含本地路径）
.tooling/
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/signing.properties
android/.idea/
*.jks
*.keystore
*.keystore.id

# 发布产物（历史版本压缩包与 APK 不进 git，发布走 GitHub Releases）
release/*.apk
release/*.aab
release/qa/
fangcun-release-*.tar.gz
fangcun-gpt-review-package-*.zip

# 内部评审与交付报告（含个人部署上下文，不适合公开）
review-package/
docs/v2.2-internal-audit.md
docs/方寸-v2.2-最终交付报告.md

# 个人真实数据（只留在本地，绝不进 git；示例在 imports/examples/）
/imports/*.json

# 内部归档与一次性脚本（永不入库）
aicb-release-calendar3/
release/
归档.zip
launch-codex.sh
launch-refine.sh
smoke-bisect-root.js
